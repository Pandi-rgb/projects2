# TODO

- [x] Identifikasi masalah di `assets/navbar/profile.html` (tag `<img>` invalid dengan closing `</img>`)
- [x] Mengedit `assets/navbar/profile.html` untuk memperbaiki tag `<img>` (hapus `</img>` dan gunakan self-closing `<img ... />`), tanpa mengubah attribute/class/teks lain
- [ ] (Follow-up) Jalankan/cek halaman `assets/navbar/profile.html` di browser untuk memastikan render normal
